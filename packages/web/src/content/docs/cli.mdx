---
title: CLI
description: Conduit CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The Conduit CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
conduit
```

But it also accepts commands as documented on this page. This allows you to interact with Conduit programmatically.

```bash
conduit run "Explain how closures work in JavaScript"
```

---

### tui

Start the Conduit terminal user interface.

```bash
conduit [project]
```

#### Flags

| Flag                                        | Short | Description                                                             |
| ------------------------------------------- | ----- | ----------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr>    | `-c`  | Continue the last session                                               |
| <nobr><code>{"--session"}</code></nobr>     | `-s`  | Session ID to continue                                                  |
| <nobr><code>{"--fork"}</code></nobr>        |       | Fork the session when continuing (use with `--continue` or `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>      |       | Prompt to use                                                           |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use in the form of provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>       |       | Agent to use                                                            |
| <nobr><code>{"--port"}</code></nobr>        |       | Port to listen on                                                       |
| <nobr><code>{"--hostname"}</code></nobr>    |       | Hostname to listen on                                                   |
| <nobr><code>{"--mdns"}</code></nobr>        |       | Enable mDNS discovery                                                   |
| <nobr><code>{"--mdns-domain"}</code></nobr> |       | Custom mDNS domain name                                                 |
| <nobr><code>{"--cors"}</code></nobr>        |       | Additional browser origin(s) to allow CORS                              |

---

## Commands

The Conduit CLI also has the following commands.

---

### agent

Manage agents for Conduit.

```bash
conduit agent [command]
```

---

#### create

Create a new agent with custom configuration.

```bash
conduit agent create
```

This command will guide you through creating a new agent with a custom system prompt and permission configuration. Anything you don't allow is denied in the generated agent's frontmatter.

#### Flags

| Flag                                        | Short | Description                                                                                                                                                                                                                |
| ------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--path"}</code></nobr>        |       | Directory to write the agent file to (defaults to global or `.conduit/agent` based on the prompt)                                                                                                                         |
| <nobr><code>{"--description"}</code></nobr> |       | What the agent should do                                                                                                                                                                                                   |
| <nobr><code>{"--mode"}</code></nobr>        |       | Agent mode: `all`, `primary`, or `subagent`                                                                                                                                                                                |
| <nobr><code>{"--permissions"}</code></nobr> |       | Comma-separated list of permissions to allow (default: all). Available: `bash`, `read`, `edit`, `glob`, `grep`, `webfetch`, `task`, `todowrite`, `websearch`, `lsp`, `skill`. Anything omitted is denied. Alias: `--tools` |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use, in `provider/model` format                                                                                                                                                                                   |

Passing all of `--path`, `--description`, `--mode`, and `--permissions` runs the command non-interactively.

---

#### list

List all available agents.

```bash
conduit agent list
```

---

### attach

Attach a terminal to an already running Conduit backend server started via `serve` or `web` commands.

```bash
conduit attach [url]
```

This allows using the TUI with a remote Conduit backend. For example:

```bash
# Start the backend server for web/mobile access
conduit web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
conduit attach http://10.20.30.40:4096
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory to start TUI in                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `CONDUIT_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `CONDUIT_SERVER_USERNAME` or `conduit`) |

---

### auth

Command to manage credentials and login for providers.

```bash
conduit auth [command]
```

---

#### login

Conduit is powered by the provider list at [Models.dev](https://models.dev), so you can use `conduit auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/conduit/auth.json`.

```bash
conduit auth login
```

When Conduit starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

##### Flags

| Flag                                     | Short | Description                                          |
| ---------------------------------------- | ----- | ---------------------------------------------------- |
| <nobr><code>{"--provider"}</code></nobr> | `-p`  | Provider ID or name to log in to                     |
| <nobr><code>{"--method"}</code></nobr>   | `-m`  | Login method label to use, skipping method selection |

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
conduit auth list
```

Or the short version.

```bash
conduit auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
conduit auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
conduit github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
conduit github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
conduit github run
```

##### Flags

| Flag                                  | Description                            |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event to run the agent for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
conduit mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
conduit mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
conduit mcp list
```

Or use the short version.

```bash
conduit mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
conduit mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
conduit mcp auth list
```

Or use the short version.

```bash
conduit mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
conduit mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
conduit mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
conduit models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
conduit models anthropic
```

#### Flags

| Flag                                    | Description                                                  |
| --------------------------------------- | ------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Refresh the models cache from models.dev                     |
| <nobr><code>{"--verbose"}</code></nobr> | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in Conduit.

```bash
conduit models --refresh
```

---

### run

Run conduit in non-interactive mode by passing a prompt directly.

```bash
conduit run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "conduit run"
conduit run Explain the use of context in Go
```

You can also attach to a running `conduit serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
conduit serve

# In another terminal, run commands that attach to it
conduit run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag                                                         | Short | Description                                                                |
| ------------------------------------------------------------ | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>                      |       | The command to run, use message for args                                   |
| <nobr><code>{"--continue"}</code></nobr>                     | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>                      | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>                         |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--share"}</code></nobr>                        |       | Share the session                                                          |
| <nobr><code>{"--model"}</code></nobr>                        | `-m`  | Model to use in the form of provider/model                                 |
| <nobr><code>{"--agent"}</code></nobr>                        |       | Agent to use                                                               |
| <nobr><code>{"--file"}</code></nobr>                         | `-f`  | File(s) to attach to message                                               |
| <nobr><code>{"--format"}</code></nobr>                       |       | Format: default (formatted) or json (raw JSON events)                      |
| <nobr><code>{"--title"}</code></nobr>                        |       | Title for the session (uses truncated prompt if no value provided)         |
| <nobr><code>{"--attach"}</code></nobr>                       |       | Attach to a running conduit server (e.g., http://localhost:4096)          |
| <nobr><code>{"--password"}</code></nobr>                     | `-p`  | Basic auth password (defaults to `CONDUIT_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr>                     | `-u`  | Basic auth username (defaults to `CONDUIT_SERVER_USERNAME` or `conduit`) |
| <nobr><code>{"--dir"}</code></nobr>                          |       | Directory to run in, or path on the remote server when attaching           |
| <nobr><code>{"--port"}</code></nobr>                         |       | Port for the local server (defaults to random port)                        |
| <nobr><code>{"--variant"}</code></nobr>                      |       | Model variant (provider-specific reasoning effort)                         |
| <nobr><code>{"--thinking"}</code></nobr>                     |       | Show thinking blocks                                                       |
| <nobr><code>{"--dangerously-skip-permissions"}</code></nobr> |       | Auto-approve permissions that are not explicitly denied (dangerous!)       |

---

### serve

Start a headless Conduit server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
conduit serve
```

This starts an HTTP server that provides API access to conduit functionality without the TUI interface. Set `CONDUIT_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `conduit`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### session

Manage Conduit sessions.

```bash
conduit session [command]
```

---

#### list

List all Conduit sessions.

```bash
conduit session list
```

##### Flags

| Flag                                      | Short | Description                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limit to N most recent sessions      |
| <nobr><code>{"--format"}</code></nobr>    |       | Output format: table or json (table) |

---

#### delete

Delete an Conduit session.

```bash
conduit session delete <sessionID>
```

---

### stats

Show token usage and cost statistics for your Conduit sessions.

```bash
conduit stats
```

#### Flags

| Flag                                    | Description                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Show stats for the last N days (all time)                                   |
| <nobr><code>{"--tools"}</code></nobr>   | Number of tools to show (all)                                               |
| <nobr><code>{"--models"}</code></nobr>  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| <nobr><code>{"--project"}</code></nobr> | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
conduit export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

#### Flags

| Flag                                     | Description                           |
| ---------------------------------------- | ------------------------------------- |
| <nobr><code>{"--sanitize"}</code></nobr> | Redact sensitive transcript/file data |

---

### import

Import session data from a JSON file or Conduit share URL.

```bash
conduit import <file>
```

You can import from a local file or an Conduit share URL.

```bash
conduit import session.json
conduit import https://opncd.ai/s/abc123
```

---

### web

Start a headless Conduit server with a web interface.

```bash
conduit web
```

This starts an HTTP server and opens a web browser to access Conduit through a web interface. Set `CONDUIT_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `conduit`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
conduit acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--cwd"}</code></nobr>         | Working directory                          |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### plugin

Install a plugin and update your config.

```bash
conduit plugin <module>
```

Or use the alias.

```bash
conduit plug <module>
```

#### Flags

| Flag                                   | Short | Description                     |
| -------------------------------------- | ----- | ------------------------------- |
| <nobr><code>{"--global"}</code></nobr> | `-g`  | Install in global config        |
| <nobr><code>{"--force"}</code></nobr>  | `-f`  | Replace existing plugin version |

---

### pr

Fetch and checkout a GitHub PR branch, then run Conduit.

```bash
conduit pr <number>
```

---

### db

Database tools.

```bash
conduit db [query]
```

#### Flags

| Flag                                   | Description                    |
| -------------------------------------- | ------------------------------ |
| <nobr><code>{"--format"}</code></nobr> | Output format: `json` or `tsv` |

---

#### path

Print the database path.

```bash
conduit db path
```

---

### debug

Debugging and troubleshooting tools.

```bash
conduit debug [command]
```

---

### uninstall

Uninstall Conduit and remove all related files.

```bash
conduit uninstall
```

#### Flags

| Flag                                        | Short | Description                                 |
| ------------------------------------------- | ----- | ------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Keep configuration files                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Keep session data and snapshots             |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Show what would be removed without removing |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates conduit to the latest version or a specific version.

```bash
conduit upgrade [target]
```

To upgrade to the latest version.

```bash
conduit upgrade
```

To upgrade to a specific version.

```bash
conduit upgrade v0.1.48
```

#### Flags

| Flag                                   | Short | Description                                                       |
| -------------------------------------- | ----- | ----------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The conduit CLI takes the following global flags.

| Flag                                       | Short | Description                          |
| ------------------------------------------ | ----- | ------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Display help                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Print version number                 |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Print logs to stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Log level (DEBUG, INFO, WARN, ERROR) |
| <nobr><code>{"--pure"}</code></nobr>       |       | Run without external plugins         |

---

## Environment variables

Conduit can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `CONDUIT_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `CONDUIT_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `CONDUIT_CONFIG`                     | string  | Path to config file                               |
| `CONDUIT_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `CONDUIT_CONFIG_DIR`                 | string  | Path to config directory                          |
| `CONDUIT_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `CONDUIT_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `CONDUIT_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `CONDUIT_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `CONDUIT_PERMISSION`                 | string  | Inlined json permissions config                   |
| `CONDUIT_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `CONDUIT_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `CONDUIT_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `CONDUIT_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `CONDUIT_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `CONDUIT_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `CONDUIT_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `CONDUIT_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `CONDUIT_DISABLE_MOUSE`              | boolean | Disable mouse capture in the TUI                  |
| `CONDUIT_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `CONDUIT_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `CONDUIT_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `CONDUIT_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `CONDUIT_SERVER_USERNAME`            | string  | Override basic auth username (default `conduit`) |
| `CONDUIT_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `CONDUIT_EXPERIMENTAL`                         | boolean | Enable the experimental umbrella flag   |
| `CONDUIT_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `CONDUIT_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `CONDUIT_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `CONDUIT_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `CONDUIT_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `CONDUIT_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `CONDUIT_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `CONDUIT_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `CONDUIT_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `CONDUIT_EXPERIMENTAL_LSP_TY`                  | boolean | Enable TY LSP for python files          |
| `CONDUIT_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
| `CONDUIT_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Enable background subagent tasks        |
| `CONDUIT_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Enable experimental event system        |
| `CONDUIT_EXPERIMENTAL_NATIVE_LLM`              | boolean | Enable native LLM request path          |
| `CONDUIT_EXPERIMENTAL_PARALLEL`                | boolean | Enable parallel web search execution    |
| `CONDUIT_EXPERIMENTAL_SCOUT`                   | boolean | Enable Scout subagent                   |
| `CONDUIT_EXPERIMENTAL_WORKSPACES`              | boolean | Enable workspace support                |
